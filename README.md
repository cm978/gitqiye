这是一个动作识别和虚拟交互系统，但是现在只做了一个demo，
1.也就是说只能看看效果，然后细节部分的内容就不是很完善，感觉有很多内容上的问题
2.目前没有用到模型识别这种东西，现在只提供测试
3.项目预期路径（也就是ResNet-50和LSTM这个模型没跑）：
MediaPipe 手部 ROI 裁剪
  ↓
ResNet-50 CNN 提取每帧特征
  ↓
LSTM 识别动态手势
  ↓
保存可复用权重目录
  ↓
Flask 自动加载该目录用于动态手势识别
